---
title: Rabbit Relay
description: A stable, type-safe RabbitMQ framework for Node.js
icon: Rabbit
---

Rabbit Relay builds on `amqplib` and deliberately keeps exchanges, queues,
bindings, routing keys, acknowledgements, retries, dead-letter queues, and
confirms **explicit**. No implicit magic.

```ts
import { RabbitMQBroker, event } from "@bitspacerlabs/rabbit-relay";

const broker = new RabbitMQBroker("orders_service");

const api = await broker
  .queue("orders.q")
  .exchange<{ orderCreated: EventEnvelope<OrderCreated> }>("orders.ex", {
    exchangeType: "topic",
    routingKey: "orders.*",
    publisherConfirms: true,
  })
  .with({ orderCreated });

api.handle("orderCreated", async (_id, ev) => {
  await fulfill(ev.data);
});

await api.consume({ prefetch: 20 });
```

## Why Rabbit Relay?

<div className="grid grid-cols-1 md:grid-cols-2 gap-4 not-prose my-6">
  <div className="rounded-xl border bg-fd-card p-5">
    <h3 className="font-semibold mb-2">Type-safe events</h3>
    <p className="text-sm text-fd-muted-foreground">Event factories and typed envelopes. Wildcard handlers narrow via discriminated unions, no casts.</p>
  </div>
  <div className="rounded-xl border bg-fd-card p-5">
    <h3 className="font-semibold mb-2">Bounded retries</h3>
    <p className="text-sm text-fd-muted-foreground">Fixed or exponential backoff. All broker-native TTL queues, never in-memory timers.</p>
  </div>
  <div className="rounded-xl border bg-fd-card p-5">
    <h3 className="font-semibold mb-2">Dead-letter pipelines</h3>
    <p className="text-sm text-fd-muted-foreground">DLX/DLQ wiring in one config block, with observability headers on every retry copy.</p>
  </div>
  <div className="rounded-xl border bg-fd-card p-5">
    <h3 className="font-semibold mb-2">Explicit topology</h3>
    <p className="text-sm text-fd-muted-foreground">Exchanges, queues and bindings visible in code, plans, and the CLI diff tool.</p>
  </div>
</div>

<Cards>
  <Card title="Quickstart" description="Publish your first type-safe event in minutes." href="/docs/quickstart" />
  <Card title="Retry & Dead-Letter Queues" description="Bounded retries, backoff strategies, and poison-pill handling." href="/docs/retry-dlq" />
  <Card title="Exponential Backoff" description="Broker-native doubling waits for transient outages." href="/docs/exponential-backoff" />
</Cards>

## AI & Agent Tooling

Rabbit Relay ships its documentation for machines as well as humans:

<Cards>
  <Card title="llms.txt" description="Curated index of every docs page, optimized for LLM context windows." href="/llms.txt" />
  <Card title="llms-full.txt" description="All docs as clean markdown in one file. Perfect for retrieval-augmented generation." href="/llms-full.txt" />
  <Card title="TypeScript types" description="Full type definitions shipped in the npm package. Agents can import and reason about types." href="/docs/api/rabbitmq-broker" />
</Cards>
